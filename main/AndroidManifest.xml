<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.animalsentimentsapp1">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:exported="false"
        android:grantUriPermissions="true"
        android:icon="@mipmap/dogs_and_cats"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/dogs_and_cats"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnimalSentimentsApp1">
        <activity android:name=".Upload2"></activity>
        <activity android:name=".Registration"></activity>
        <activity android:name=".Result" />
        <activity android:name=".Upload" />
        <activity android:name=".SelectPet" />
        <activity android:name=".MainActivity">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>